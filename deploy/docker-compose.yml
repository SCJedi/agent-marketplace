version: '3.8'
services:
  marketplace:
    build: ..
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_NAME=MyNode
      - OPEN_BROWSER=false
    volumes:
      - marketplace-data:/app/data
    restart: unless-stopped

volumes:
  marketplace-data:
